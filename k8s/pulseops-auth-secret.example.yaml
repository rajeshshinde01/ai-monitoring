# Template only: do not commit a real client secret to Git.
# Create the actual Secret through your approved platform or secret-management process.
apiVersion: v1
kind: Secret
metadata:
  name: pulseops-auth
  namespace: mindspark-official
type: Opaque
stringData:
  oidc-client-secret: REPLACE_WITH_SECRET_MANAGER_VALUE
  session-secret: REPLACE_WITH_A_LONG_RANDOM_VALUE
  # Add only when local break-glass administration is explicitly enabled.
  break-glass-password: REPLACE_WITH_12_PLUS_CHARACTER_SECRET
